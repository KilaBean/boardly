// Stub for the "server-only" package, which throws when imported outside a
// React Server Component bundle. Tests exercise server modules directly, so
// the guard is aliased away here. It remains active in real builds.
export {};
